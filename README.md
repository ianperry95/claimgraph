# claimgraph
Claims-backed knowledge graph memory plugin for OpenClaw. Entities are stable identity anchors; mutable knowledge lives in versioned claims with provenance. 3-tier hybrid dedup, type-aware staleness, and graph traversal over SurrealDB. Drop-in single-user self-hosted alternative to pure vector memory.
